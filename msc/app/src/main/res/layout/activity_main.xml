<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/connect_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="about"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/inverse_kinematics"
        app:layout_constraintEnd_toEndOf="@id/inverse_kinematics"
        app:layout_constraintStart_toStartOf="@id/inverse_kinematics"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <Button
        android:id="@+id/inverse_kinematics"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="inverse kinematics"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/motion_sensor_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/connect_button" />

    <Button
        android:id="@+id/motion_sensor_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="motion control"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/reset_button"
        app:layout_constraintEnd_toEndOf="@id/inverse_kinematics"
        app:layout_constraintStart_toStartOf="@id/inverse_kinematics"
        app:layout_constraintTop_toBottomOf="@id/inverse_kinematics" />

    <Button
        android:id="@+id/reset_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="Ambient Light"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/inverse_kinematics"
        app:layout_constraintStart_toStartOf="@id/inverse_kinematics"
        app:layout_constraintTop_toBottomOf="@id/motion_sensor_button" />



</androidx.constraintlayout.widget.ConstraintLayout>